#!/bin/bash
# Redirect to setup -- homebrew (same behavior; kept for backward compatibility / old links).
# When run via curl, re-fetch and run setup with homebrew.
SETUP_URL="https://raw.githubusercontent.com/Naoya-Studio/dotfiles/main/script/setup"
exec bash -c "$(curl -fsSL "$SETUP_URL")" -- homebrew
